import type { SVGProps } from 'react';

export const BosiCapitalLogo = (props: SVGProps<SVGSVGElement>) => (
  // This component is no longer used for the main logo, which is now /logo.jpg.
  // You can place other SVG icons here if needed.
  <svg
    xmlns="http://www.w3.org/2000/svg"
    viewBox="0 0 24 24"
    fill="currentColor"
    {...props}
  ></svg>
);
