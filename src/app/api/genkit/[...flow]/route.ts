// This file has been intentionally disabled to resolve a critical build
// error caused by a dependency conflict between @genkit-ai/next and the
// version of Next.js used in this project.
//
// The AI functionality in this application is handled via Next.js Server Actions,
// so this API route is not required for the app to function correctly.

/*
import { createNextApi } from '@genkit-ai/next';
import { ai } from '@/ai/genkit';

// This import is crucial for Next.js to discover and register your flows.
// Make sure to import all files that define flows.
import '@/ai/flows/due-date-monitoring-tool';

export const { GET, POST } = createNextApi({
  ai,
});
*/
